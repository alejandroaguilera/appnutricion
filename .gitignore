# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# env
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/dev.db

# local deploy secrets (Dokploy ids, DB password, seed token)
.env.dokploy
.claude/scheduled_tasks.lock

# datos de salud reales del atleta — nunca al repo público, se coloca a mano
# en el servidor antes de correr el seed (ver decisión #1 del plan)
BodyComposition_*.csv

# credenciales que Alejandro deja en la raíz para que las cargue a Dokploy —
# el repo es público, aquí nunca entra un secreto
Tokens.md

# capturas de referencia y fotos de comida del atleta
foto*.png

# el plan de nutrición y el PDF de la nutrióloga: peso, % de grasa, nombre del
# atleta y teléfono de la consulta. Son la FUENTE de lo que se siembra, no algo
# que el repo público deba guardar — el resultado ya vive en lib/data/.
03-PLAN-NUTRICION.md
*.pdf
